{
  "name": "cypress_pom",
  "version": "1.0.0",
  "description": "Cypress POM framework",
  "main": "index.js",
  "scripts": {
    "test:addToCart": "npx cypress run --spec cypress/e2e/tests/*.cy.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gowthami Endala",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  },
  "dependencies": {
    "chai": "^5.1.1"
  }
}
